mutation CreateRedemptionCode($input: CreateRedemptionCodeInput!) {
  createRedemptionCode(input: $input) {
    id
    code
    plan
    trialDays
    maxUses
    usedCount
    expiresAt
    note
    createdAt
  }
}
