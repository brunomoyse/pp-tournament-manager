mutation OnboardClub($input: OnboardClubInput!) {
  onboardClub(input: $input) {
    token
    user {
      id
      email
      username
      firstName
      lastName
      role
      clubRole
      managedClub {
        id
        name
        plan
      }
    }
    club {
      id
      name
      plan
    }
  }
}
