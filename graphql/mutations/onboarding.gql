mutation OnboardClub($input: OnboardClubInput!) {
  onboardClub(input: $input) {
    token
    user {
      id
      email
      username
      firstName
      lastName
      role
      managedClub {
        id
        name
      }
    }
    club {
      id
      name
    }
  }
}
